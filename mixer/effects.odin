package mixer

AudioEffect :: struct {}

init_effect :: proc() -> AudioEffect {
    assert(false, "TO DO!");
    return {};
}

dump_effekt :: proc(using effect: ^AudioEffect) {
    assert(false, "TO DO!");
}