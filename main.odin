package ex_utils
/*

import "core:log"

import image "image"

main :: proc() {
    context.logger = log.create_console_logger()

    using image;

    image := ImageBGR{};
    image.size = IMAGE_SIZE(50, 50);
    generate_random_image2(&image);
    write(&image, FileWriteDescriptor{
        file_path = "bgr.ppm",
        write_format = .PPM,
    });
}*/